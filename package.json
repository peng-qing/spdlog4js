{
  "name": "spdlog4js",
  "version": "1.0.0",
  "description": "封装 C++ 的 `spdlog` 提供给 `nodejs` 服务使用。",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "rebuild": "node-gyp rebuild",
    "build": "node-gyp clean && node-gyp configure && node-gyp build --verbose",
    "prebuild": "prebuildify --napi --strip"
  },
  "keywords": [
    "spdlog",
    "nodejs",
    "napi"
  ],
  "author": "pengqq",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "node-addon-api": "^8.7.0",
    "node-gyp-build": "^4.8.4"
  },
  "devDependencies": {
    "node-gyp": "^12.2.0",
    "prebuildify": "^6.0.1",
    "typescript": "^6.0.2"
  }
}